package:
  name: tass-converter
  version: 1.1.0

source:
  path: ../

build:
  noarch: python
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - python
    - setuptools
    - pip
  run:
    - python >=3.13
    - openpyxl

extra:
  authors:
    - name: Dominic Parent
      email: dominic.parent@statcan.gc.ca
    - name: Tyler Desjardins
      email: josephgillestyler.desjardins@statcan.gc.ca
